% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare data.R
\name{prepare_wide}
\alias{prepare_wide}
\title{Aggregate panel to \code{cohort} level or prepare for the \code{unit} level.}
\usage{
prepare_wide(panel, level = "cohort", boot = FALSE)
}
\arguments{
\item{panel}{List. The list should contain:
\itemize{
\item \code{Y}    Dataframe or numeric matrix. This is a wide panel dataframe of outcomes with adoption date column being the first one. \code{Y} can be a matrix when the estimation is conducted on the \code{unit} level.
\item \code{X}    Dataframe. This is a dataframe with auxiliary data like adoption date, weights and covariates.
or
\item \code{Y_wt} Dataframe. This is Y dataframe with merged X dataframe. It contains
\item \code{W}    Dataframe, numeric matrix or NULL. This is a wide panel dataframe of treatment indicators with adoption date column being the first one. NULL can be passed when boot is TRUE. Numeric matrix can be passed when the estimation is conducted on the \code{unit} level.
}}

\item{level}{Character. The level should be \code{unit} or \code{cohort}. The data is not being aggregated when the level is "unit".}

\item{boot}{Bool. If TRUE, W_avg matrix is not calculated to speed up the bootstraping for standard error estimation.}
}
\value{
\code{panel_avg} List. The list contains:
-\code{Y_avg}:          Numeric matrix. The N x T matrix of outcomes.
-\code{W_avg}:          Binary or boolen matrix. The N x T matrix of treatment indicators. The matrix has a stairlike structure with treated cells being in the bottom.
-\code{coh_weights}:    Numeric vector. The N x 1 cohort weights vector (the number of units in cohorts or the cohort weights).
}
\description{
The function prepares the data for sequential_estimator.
For "unit" level the function removes adoption date column in Y and W. Y and W can be both matrices without \code{adopt_date} column.
For "cohort" level the function aggregates the outcome and weights variable by adoption date and covariates.
}
